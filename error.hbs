{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}
<div class="row page-row">

      <div class="tag_head_wrap max-content-width home-style">
        <div class="text-center">
          <img src="{{asset "images/error.png"}}" alt="{{t "Error"}}" title="{{t "Error"}}" class="tag-image" />
          <h1 class="text-center tag-title">{{t "Something went wrong..."}}</h1>    
        </div>
      </div>
      <div class="main-content text-center">
        <pre>{{statusCode}} — {{message}}</pre>
        {{t "This post/page doesn't exist anymore or has been made unavailable."}}
        <div class="card-actions-bottom">
          <a class="button-url-bg" href="/">{{t "Return to the homepage"}}</a>
        </div>
      </div>
</div>